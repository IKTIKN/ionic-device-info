import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'iktikn.ionic.device',
  appName: 'Device info',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
